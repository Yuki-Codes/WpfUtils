<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">


	<ControlTemplate x:Key="WpfUtilsSeparatorTemplate" TargetType="{x:Type Separator}">
		<Rectangle Fill="{TemplateBinding Foreground}" SnapsToDevicePixels="true" />
	</ControlTemplate>

	<Style x:Key="WpfUtilsSeparator" TargetType="{x:Type Separator}">
		<Setter Property="Background" Value="Transparent" />
		<Setter Property="BorderThickness" Value="1" />
		<Setter Property="Foreground" Value="{DynamicResource ForegroundLightBrush}" />
		<Setter Property="Margin" Value="4" />
		<Setter Property="MinHeight" Value="1" />
		<Setter Property="MinWidth" Value="1" />
		<Setter Property="Opacity" Value="0.5" />
		<Setter Property="Template" Value="{StaticResource WpfUtilsSeparatorTemplate}" />
	</Style>

</ResourceDictionary>
